{
  "name": "web-sensors",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/imohanvadivel/Web-Sensors.git",
  "author": "Mohan Vadivel <mohanvadivel@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild src/script.js src/style.css --bundle --watch --sourcemap --outdir=./dist --external:*.woff --external:*.woff2 --external:*.ttf --external:*.otf",
    "build": "esbuild src/script.js src/style.css --bundle --minify --outdir=./dist --external:*.woff --external:*.woff2 --external:*.ttf --external:*.otf"
  },
  "devDependencies": {
    "esbuild": "^0.12.9"
  }
}
